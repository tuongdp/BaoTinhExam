{
  "installCommand": "npm ci --include=dev",
  "buildCommand": "npm run build -w apps/frontend",
  "outputDirectory": "apps/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
